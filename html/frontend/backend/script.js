function area_circle(raio) {
    return PI * raio * raio;
    var area = PI * (raio **2);   

 return area 
}

function area_quadrado(lado) {
    var area = lado * lado;
     return area;
}


console.log(`Area do circulo: ${area_circle(5)}`);
console.log(`Area do retangulo: ${area_retangulo(5)}`);
console.log(`Area do triangulo: ${area_triangulo(5)}`);
